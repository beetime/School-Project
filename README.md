# Jenkins Language Pipeline Task

This project demonstrates a Jenkins Pipeline that:
- Pulls code from GitHub
- Allows the user to choose a programming language
- Prints the selected language
- Sends an email with the result
Each file prints a hello message.

## Jenkins
Jenkins runs inside Docker on a VM and connects to this repository using SSH.
